// Faça um programa para imprimir todo o alfabeto(A-Z) (maiúscula)  usando repetição (while) e a tabela ASCII(Tabela ASCII (up.pt)).
#include <stdio.h>

void main() {
    int x = 65;
    while (x < 91) {
        printf("%c\n", x);
        x++;
    }
}
